# Images

Screenshots and image assets referenced from other docs in this tree.

## Conventions
- Use descriptive lowercase filenames (e.g. `live-webhook-ingestion.png`).
- Reference images with a relative path from the doc that uses them: `![alt](../images/foo.png)`.
- Keep raw exports / oversized originals out — compress before committing.
