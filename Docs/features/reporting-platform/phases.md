# Reporting Platform — Phases

> Phase-by-phase build tracker. Detail per phase lands in `phase-<n>-implementation.md`
> as it's built. When every phase is complete, consolidate to the archived shape
> (`README.md` + `plan.md` + `implementation-log.md`).
>
> Plan: [`plan.md`](./plan.md). Architecture: [RD-012](../../repo-decisions/RD-012-reporting-platform-architecture.md).

| Phase | Status | Summary |
|-------|--------|---------|
| 0 — Foundation (docs) | Done | Research/findings consolidated, RD-012 (Proposed), plan + this tracker, the "add a report" recipe. No code. |
| 1 — Dashboard metrics (direct slice) | **Built (2026-06-24, on `feature/reporting-platform`)** — [backend](./phase-1-backend-implementation.md) + [frontend](./phase-1-frontend-implementation.md) | Operational pipeline health, **all over one 24h window**: windowed run counts + success rate + deltas, hourly throughput, **operational** funnel `Ingested→Domain Events→Workflow Runs→Failed` (runs-only scope, **volume counts only — no conversion %s**), view-only failures worklist, recent runs, events/min, and a **3-state health headline** (HEALTHY/DEGRADED/UNHEALTHY) on success rate. **One read-only snapshot endpoint (`GET /admin/dashboard/snapshot`) + a dashboard-local JDBC read repo + a DTO — no framework.** BE (read repo+IT, snapshot service/controller) + FE (port/adapter/contract, full-width handoff UI w/ custom AreaChart+Sparkbars). Backend suite + UI gate green. **Known: board is static within the clock hour (floor-to-hour); rolling-window fix deferred.** Not yet merged to `dev`. |
| **2a — Lead-timeline substrate + semantic layer** (the reporting foundation) | **DONE 2026-08-13** — `V24` holder-intervals view, IT green (11/11). Source normalization is **Java, not SQL** (`LeadSourceNormalizer`, 4 tests green) — a `V25` view was built and reverted, since ad channels are company data, not schema — [phase-2-implementation.md](./phase-2-implementation.md) §"Phase 2a" ([scoping](./phase-2-accountability-scoping.md), [RD-014](../../repo-decisions/RD-014-reporting-query-architecture.md)) | Reconstruct a **per-lead ownership timeline** as an on-read SQL view `v_lead_holder_intervals` (holder-at-any-instant), from `persons` (current owner + `created_at` anchor) + `person.state_changed` reassignment events — **not** `person.created` (only 34% coverage). Also encodes the **canonical lead-source mapping** (raw free-text → ~12 buckets; [data-truths §2.7](./findings/data-truths.md)) as part of the semantic layer. One Flyway view (`V24`) + a Testcontainers IT proving reassigned-lead interval correctness. **No API surface.** Verified against the FUB API (2026-07-02). |
| **2b — The two reports** (on the timeline) | **Backend DONE 2026-08-14** — R1 `GET /admin/reporting/source-contact`, R2 `GET /admin/reporting/accountability` (+ `/{agentId}/unreached`). 52 new tests incl. end-to-end HTTP. **FE (2f) in build 2026-08-14** — design handoff received (`ui/Flux Design System/design_handoff_reporting_phase2/`), plan approved: one `modules/reports/` module, ECharts sankey per [RD-015](../../repo-decisions/RD-015-charting-library-echarts.md), handoff copy adjusted to calendar windows + Spoke/Attempted/Untouched — [phase-2-implementation.md](./phase-2-implementation.md) §"Phase 2f — Frontend plan" | **R1 — Source → Agent → contact outcome** (coverage; snapshot flags; surfaces Facebook 51% vs Instagram 92%) and **R2 — Agent accountability** (assigned → contacted, **timeline-correct** — each contact credited to the holder-at-the-time, immune to reassignment). **Contact model + windows rewritten 2026-08-12 (replan):** contact means a **conversation** — an outbound call over the threshold (reusing the existing `classifyCall` ladder) **by the holder at that instant**; only **31% of outbound calls qualify**. FUB's `contacted` flag is demoted to **"Attempted"** — it overstates by 27% (of 532 flagged leads, 143 had only failed dials). Three states: **Spoke / Attempted / Nothing**. Windows are **calendar days in the business timezone** — Yesterday / Today / This week, extensible to month/year with no new code. Owner `uid=1` = a normal agent. Rejected and recorded: nightly snapshot table, stamping the holder onto calls. BE-first (read repos + APIs + ITs); FE both modules after the design handoff. Direct slice — plain SQL, **not** the RD-012 framework; **no** materialized read-model (Phase 3). **R2 accuracy note:** the ingress that caused the audit's ~40% false reds is now **fixed** (2c — verified 2026-07-02, steady hourly ingestion on Railway), so R2 is trustworthy for windows **entirely after the fix**. We are **not** backfilling pre-fix history, so don't present R2 over a window reaching into the pre-fix zero-days — it self-heals as the 24h/7d window rolls past the fix date. |
| 2c — Reliable webhook ingress (**DONE**) · reconcile/backfill (**PARKED — not doing**) | **Ingress: DONE 2026-07-02** · Reconcile: **parked** | **Ingress FIXED:** the app runs on Railway at a stable public URL with FUB webhooks registered to it — verified 2026-07-02 (steady hourly ingestion; the earlier ephemeral Cloudflare quick-tunnel root cause is resolved). Forward data is now reliable → R2 is trustworthy for post-fix windows. **Reconcile/backfill = PARKED (decided 2026-07-02): we are NOT building it.** It would only recover *pre-fix* history or insure against occasional Railway deploy-blip drops. **⚠ Parked caveat (record so we don't forget) — for IF we ever build it:** it must be a **silent, data-only insert**, because the existing upsert path emits domain events that drive the workflow engine, so a naive backfill would **retroactively fire automations (tasks/notes) for old calls**. It would also need **bulk-list FUB client methods** (`/v1/calls`+`/v1/people` since-last-sync + pagination) and a **`sync_state` watermark table** — none of which exist today. |
| 2d — Team/agent roster (FUB-user ingestion, Issue #19) | Not started — **new 2026-07-02, non-blocking enhancement** | Ingest the real FUB team roster (`GET /v1/users` → an agents/users table) so the accountability board gets a **complete agent denominator** (surfaces idle / zero-lead agents), can **name a non-assignee caller**, and gets the structural fix for the **broken `kind` classifier** (540 agents → UNKNOWN). **The one new-capture item** in an otherwise on-read slice — does **not** block 2a/2b (the assignee's name is already free via `assignedTo`). Build before or after the reports. |
| 2e — Index + client cache | Not started — small, and only after measuring | A JSONB expression index on `assignedUserId`, and a longer `staleTime` for reporting queries than the app-wide 15s (a calendar-day window does not change minute to minute). Measured baseline: R1 ~199ms / R2 ~96ms at **5× the brokerage's whole lead count**, so this is tuning, not rescue. |
| 3 — Extract platform framework | Not started | Extract `ReportProvider` / `ReportRegistry` / `ReportingQuery` port / `definitions` / generic controller **from the two working slices** (rule of three) — no behavior change to Phases 1–2. **Ratifies RD-012 → Accepted.** |
| 4 — Natural-language querying (DEFERRED · OPTIONAL · DEMAND-GATED) | Not started — **may never be built** | Per [RD-014](../../repo-decisions/RD-014-reporting-query-architecture.md): an "agent + MCP over the curated views + data-truths context," read-only, **labelled exploratory, never the source of an accountability number**. Build only if real demand for *unanticipated* questions appears (RD-014 criteria a/b/c). Filterable deterministic reports cover most apparent "ad-hoc" need without it. |

> **Sequencing note (2026-06-19 stress-test).** The framework is built **last**, extracted
> from two real reports — not first. Committing the abstraction before two working examples
> would lock in the contract at the moment of least information. See plan.md "Order of work".
> **The semantic-layer views (Phase 2a/2b) are plain SQL, not the framework** — building them now does
> not violate "extract last" (RD-014 §Relationship to RD-012).

**Deferred beyond these phases (via RD-012 seams, not in this pass):** FUB CDC mirror →
task-completion (Q5/Q6) + "useful" (Q4) + deeper funnel; **NL/agent querying (Phase 4 — demand-gated,
RD-014)**; text/SMS contact; FUB intake date. *(FUB-user ingestion moved in as Phase 2d.)*
